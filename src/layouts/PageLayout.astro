---
import "../styles/global.css";
import GoogleFont from "./GoogleFont.astro";
import ThemeScript from "./ThemeScript.astro";
import Favicon from "./Favicon.astro";
import Sidebar from "../components/Sidebar.astro";
import Header from "../components/Header.astro";
---

<!DOCTYPE html>
<html class="theme-odurgut" lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width,initial-scale=1" />
        <Favicon />
        <GoogleFont />
        <slot name="meta" />
        <ThemeScript />
    </head>

    <body>
        <!-- Mobile top bar -->
        <div class="lg:hidden border-b border-[rgb(var(--color-border-subtle))]">
            <Header />
        </div>

        <!-- Desktop: 3-column grid (sidebar | content | spacer) -->
        <div class="page-grid">
            <!-- Left: sidebar, right-aligned -->
            <div class="hidden lg:block">
                <Sidebar />
            </div>

            <!-- Center: content with full-height dotted left border -->
            <main
                id="main"
                class="min-h-screen px-5 lg:px-10 pt-0 lg:border-l lg:border-dotted lg:border-[rgb(var(--color-border-subtle))]"
            >
                <slot name="main" />
            </main>

            <!-- Right: empty spacer for symmetry -->
            <div class="hidden lg:block"></div>
        </div>
    </body>
</html>

<style>
    .page-grid {
        display: grid;
        grid-template-columns: 1fr minmax(0, 42rem) 1fr;
        min-height: 100vh;
    }

    @media (max-width: 1023px) {
        .page-grid {
            display: block;
            max-width: 42rem;
            margin: 0 auto;
        }
    }
</style>
